name: ci-fix-notifier

on:
  workflow_run:
    workflows: ["pull-request"]
    types: [completed]

jobs:
  notify:
    if: >-
      github.event.workflow_run.conclusion == 'failure' &&
      startsWith(github.event.workflow_run.head_branch, 'claude/')
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
    steps:
      - name: Find PR and post invite
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          HEAD_BRANCH: ${{ github.event.workflow_run.head_branch }}
          RUN_URL: ${{ github.event.workflow_run.html_url }}
          REPO: ${{ github.repository }}
        run: |
          # gh pr list --head expects a branch name, not a SHA.
          pr_number="$(gh pr list \
            --repo "${REPO}" \
            --head "${HEAD_BRANCH}" \
            --state open \
            --json number \
            --jq '.[0].number // ""')"
          if [[ -z "${pr_number}" ]]; then
            echo "No open PR found for ${HEAD_BRANCH} — silencing"
            exit 0
          fi
          gh pr comment "${pr_number}" \
            --repo "${REPO}" \
            --body "CI falhou ([ver run](${RUN_URL})). Responda \`/fix\` para tentar corrigir automaticamente."
