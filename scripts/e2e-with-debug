#!/bin/bash
set -o pipefail

log_file="$(pwd)/logs/e2e-$(date +%Y%m%dT%H%M%S).log"
mkdir --parents "$(dirname "${log_file}")"

LOG_FORMAT=json uv run pytest tests/e2e/ -m e2e --no-cov -v -s --log-cli-level=DEBUG -x 2>&1 | tee "${log_file}"

echo ""
echo "Log: ${log_file}"